# Copia este archivo como .env y completa los valores.
# El archivo .env está en .gitignore y NUNCA se sube al repo.

# Ruta al JSON del service account de Google Cloud (BigQuery OSM).
# Se obtiene desde Google Cloud Console > IAM > Service Accounts.
GOOGLE_APPLICATION_CREDENTIALS=/ruta/a/tu/service-account.json
